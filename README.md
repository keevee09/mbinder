# Short description

mbinder.py searches through the downloaded email file, say `all.mbox`, extracting attachments
and inline images to a folder of your choice.

# Usage:

`./mbinder.py -i all.mbox -o attachments`

The output directory will be created if it doesn't exist.
